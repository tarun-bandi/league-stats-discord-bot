{
  "name": "discord-interactions-worker",
  "private": true,
  "license": "MIT",
  "engines": { "node": ">=22" },
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "register:commands": "node scripts/register-commands.mjs",
    "smoke:alert": "node scripts/smoke-alert.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
